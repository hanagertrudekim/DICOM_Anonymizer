version: 2.1

jobs:
  build:
    # ... (이전 설정은 그대로 둠)

  deploy:
    docker:
      - image: circleci/python:3.8

    steps:
      - checkout

      - run:
          name: Deploy application
          command: bash deploy.sh

workflows:
  version: 2
  build:
    jobs:
      - build
      - deploy:
          requires:
            - build
