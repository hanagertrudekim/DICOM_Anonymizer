version: 2.1

jobs:
  build_and_deploy:
    docker:
      - image: circleci/python:3.8

    environment:
      GITHUB_TOKEN: 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
      CIRCLE_PROJECT_USERNAME: hanagertrudekim
      CIRCLE_PROJECT_REPONAME: DICOM_Anonymizer

    steps:
      - checkout

      # 빌드 스크립트
      - run:
          name: Install Dependencies and Build Application
          command: |
            sudo apt-get update
            sudo apt-get install -y python3-pip
            sudo apt-get install -y libegl1 libxkbcommon-x11-0 libxkbcommon0
            sudo apt-get install -y build-essential libgl1-mesa-dev libxkbcommon-x11-0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-render-util0 libxcb-xkb1 libxcb-xinerama0 libxcb-xfixes0 libxcb-shape0 libxcb-randr0 libxcb-sync1 libxcb-render0 libxcb-shm0 libxcb-ewmh2 libxcb-util1 libxcb-xinput0
            pip3 install -r requirements.txt
            python3 app.py

      # GitHub Release 작성
      - run:
          name: Create GitHub Release
          command: |
            github-release release --user $CIRCLE_PROJECT_USERNAME --repo $CIRCLE_PROJECT_REPONAME --tag $CIRCLE_TAG --name "Release $CIRCLE_TAG"

      # 파일 업로드
      - run:
          name: Upload Application to GitHub Release
          command: |
            github-release upload --user $CIRCLE_PROJECT_USERNAME --repo $CIRCLE_PROJECT_REPONAME --tag $CIRCLE_TAG --name "YourApp" --file path/to/your/app

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build_and_deploy:
          filters:
            tags:
              only: /^v.*/
